/** @type {import('tailwindcss').Config} */

module.exports = {
  darkMode: 'class', // This enables the class-based dark mode.
  theme: {
    extend: {
      // Add any custom colors or themes if needed
    },
  },
  plugins: [],
}
